# /etc/guacamole/guacd.conf
# guacd escucha SOLO en loopback: el único que lo toca es el proxy NestJS.
#   sudo cp deploy/guacd.conf /etc/guacamole/guacd.conf
#   sudo systemctl restart guacd

[server]
bind_host = 127.0.0.1
bind_port = 4822
